node_modules/
.jdk/
android/.gradle/
android/.kotlin/
android/app/.cxx/
android/build/
android/app/build/
# whisper.cpp is part of the Android native runtime. Keep the source in the
# repository so a fresh clone cannot silently build a non-functional stub.
.cache/
.metro/
*.log
